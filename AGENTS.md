# AGENTS.md — cara-spark

This file is the playbook for every AI coding agent working in this repository. Read it completely before touching a file. Keep it current — outdated playbooks are one of the leading causes of agent failures.

---

## Project Overview

cara-spark — brief description of what this project does and who it serves.

**Primary language(s):** unknown
**Build system:** unknown
**Test framework:** unknown

### Repository Map

```
{{REPO_MAP}}
```

Document the key directories and what they contain. Be specific: agents use this to decide where to look and where to write.

---

## Build and Test Commands

```sh
make build      # compile / build the project
make test       # run all tests (non-interactive, CI-safe)
make typecheck  # type-check without emitting (if applicable)
make dev        # run the project locally for development
```

**To run a single test file:**

```sh
make test ONE=<file>
```

Agents must run `make build && make test` before considering any implementation complete. Both must pass.

---

## Code-Style Guidelines

List the non-negotiable conventions. Be specific enough that an agent applying them for the first time will do so correctly.

**Import style:**
{{IMPORT_STYLE_NOTES}}

**Naming conventions:**
{{NAMING_CONVENTIONS}}

**Formatting / linting:**
{{FORMAT_LINT_NOTES}}
Run `make lint` to check. This must pass before any commit.

**Patterns to follow:**
- {{PATTERN_1}}
- {{PATTERN_2}}

**Anti-patterns to avoid:**
- {{ANTIPATTERN_1}}
- {{ANTIPATTERN_2}}

---

## Testing Instructions

Tests live in `tests/` using **unknown**.

**What to test:**
- {{TEST_SCOPE_1}}
- {{TEST_SCOPE_2}}

**What not to test:**
- Do not write tests that depend on network access unless the test clearly mocks it.
- Do not write tests that depend on external services without an explicit fixture or stub.

**Coverage standard:**
{{COVERAGE_STANDARD}}

---

## Security Considerations

**Secrets:**
Never commit API keys, tokens, passwords, or credentials. Use environment variables. The `.env.example` file tracks every variable the app references — keep it current.

**PHI/PII:**
{{PHI_PII_NOTES}}

**Dangerous operations:**
Migrations, deploys, deletions, and external writes are irreversible. Document the rollback plan before executing.

**Dependencies:**
{{DEPENDENCY_POLICY}}

---

## Environment Variables

All required environment variables are documented in `.env.example`. Copy it to `.env` before running locally. Never commit `.env`.

| Variable | Purpose | Required |
|---|---|---|
| {{ENV_VAR_1}} | {{ENV_VAR_1_DESC}} | {{ENV_VAR_1_REQUIRED}} |
| {{ENV_VAR_2}} | {{ENV_VAR_2_DESC}} | {{ENV_VAR_2_REQUIRED}} |

---

## Useful Context

- **Architecture decision records:** {{ADR_LOCATION}}
- **API documentation:** {{API_DOC_LOCATION}}
- **Deployment notes:** {{DEPLOY_NOTES}}

---

## Took Commands for Agents

This repo is coached by [Took](https://www.npmjs.com/package/@gettook/took). Prefer these over raw exploration:

- `took recall "<query>"` — budget-fit slice of the most relevant symbols; use it before reading whole files into context.
- `took task ready` / `took task claim <id>` / `took task close <id>` — pick and track work on the git-native task graph; reference the task in commits with a `Took-Task:` trailer.
- `took cartridge status` — check for a frozen repo-knowledge cartridge to load instead of re-deriving repo facts.
- `took diff` before committing; `took pr-ready` before opening a PR.

---

*Generated by `took init`. Run `took ready` before opening a PR. Run `took docs-check` to verify this file stays in sync with the repo.*
